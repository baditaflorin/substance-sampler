# Contributing

Thanks for helping improve Substance Sampler.

## Local Workflow

1. Install dependencies with `npm install`.
2. Install local hooks with `make install-hooks`.
3. Run `make test`, `make build`, and `make smoke` before pushing.

Use Conventional Commits for commit messages, such as `feat: add texture export`.
